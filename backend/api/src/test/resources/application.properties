# File: backend/api/src/test/resources/application.properties

# Override JWT secret for testing
# Replace this with your own generated Base64 encoded secret for testing
app.jwt.secret=dGVzdHNlY3JldGtleWZvcmp3dHRva2Vuc2luaW50ZWdyYXRpb250ZXN0c2Zvcm11bHRpcmVzdGF1cmFudGFwcA==
app.jwt.expiration-ms=3600000
app.jwt.token-prefix=Bearer

# --- Add these Stripe configurations ---
# Mock Stripe keys for tests - these don't need to be real keys for tests
stripe.secret.key=sk_test_mockSecretKeyForTests12345
stripe.publishable.key=pk_test_mockPublishableKeyForTests12345
stripe.webhook.secret=whsec_mockWebhookSecretForTests12345
# ---------------------------------------

# H2 Console for tests (if your tests involve an in-memory H2 database - adjust as needed)
spring.h2.console.enabled=false

# JPA/Hibernate settings for tests (e.g., use H2 for tests - adjust as needed)
# If your tests are not using an in-memory H2, these might be different or not needed.
spring.jpa.hibernate.ddl-auto=create-drop
spring.datasource.url=jdbc:h2:mem:testdb;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
spring.datasource.driverClassName=org.h2.Driver
spring.datasource.username=sa
spring.datasource.password=password

# Ensure Flyway is disabled for tests if you're using create-drop or an in-memory H2
spring.flyway.enabled=false

# You can also override other properties for tests if needed, e.g.,
# spring.jpa.show-sql=false
logging.level.com.multirestaurantplatform=DEBUG
app.cart.storage=db
